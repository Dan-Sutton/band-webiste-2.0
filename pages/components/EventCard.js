import React from "react";
import Link from "next/link";

function eventCard({ date, id, slug, title, updatedAt }) {
  return (
    <div>
      <Link></Link>
    </div>
  );
}

export default eventCard;
